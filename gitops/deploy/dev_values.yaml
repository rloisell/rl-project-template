# deploy/dev_values.yaml
# Ryan Loiselle — Developer / Architect | GitHub Copilot | February 2026
#
# Development environment overrides.
# Replace <LICENSE> and <APP_NAME> with actual values.

environment: dev

api:
  replicaCount: 1
  image:
    tag: "latest"                 # CI updates this to the git SHA on each build
  route:
    host: <APP_NAME>-api-<LICENSE>-dev.apps.emerald.devops.gov.bc.ca
    annotations:
      aviinfrasetting.ako.vmware.com/name: "dataclass-medium"
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: 500m
      memory: 512Mi

frontend:
  replicaCount: 1
  image:
    tag: "latest"                 # CI updates this to the git SHA on each build
  route:
    host: <APP_NAME>-<LICENSE>-dev.apps.emerald.devops.gov.bc.ca
    annotations:
      aviinfrasetting.ako.vmware.com/name: "dataclass-medium"
  config:
    apiUrl: "https://<APP_NAME>-api-<LICENSE>-dev.apps.emerald.devops.gov.bc.ca"

db:
  storage:
    size: 1Gi
